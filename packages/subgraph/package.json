{
  "name": "@0xslots/subgraph",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "deploy:studio": "graph deploy --studio 0xslots-arb-sepolia",
    "build:arbitrum": "cp subgraph.arbitrum.yaml subgraph.yaml && graph codegen && graph build",
    "deploy:arbitrum": "cp subgraph.arbitrum.yaml subgraph.yaml && graph codegen && graph build && graph deploy --studio 0-x-slots-arb",
    "create:local": "graph create --node http://localhost:8020/ 0xslots",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 0xslots"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.38.0"
  }
}
