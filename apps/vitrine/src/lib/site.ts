/** Canonical origin for this site — used for metadataBase, JSON-LD and OG urls. */
export const siteUrl = "https://0xslots.org";

export const title = "0xSlots";

export const tagline = "Property you cannot hoard";

/**
 * The studio that builds and writes for 0xSlots. Articles live in its Payload
 * CMS and are pulled in here, so the credit and the content share a source.
 */
export const studio = {
  name: "econome.studio",
  url: "https://econome.studio",
};
