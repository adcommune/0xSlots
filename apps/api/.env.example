# Alchemy RPC key for blockchain interactions
ALCHEMY_KEY=

# Chain ID for the event listener (default: 8453, Base mainnet)
# The Graph gateway API key — required; ad lookups fail without it
NEXT_PUBLIC_SUBGRAPH_API_KEY=

# Protocol chain: 84532 (Base Sepolia, default) or 8453 (Base)
CHAIN_ID=
