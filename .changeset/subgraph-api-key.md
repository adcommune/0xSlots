---
"@0xslots/sdk": patch
---

Add `subgraphApiKey` option to SDK config. When provided, sends `Authorization: Bearer <key>` header on all subgraph queries. Update Base Sepolia subgraph URL to decentralized gateway.
