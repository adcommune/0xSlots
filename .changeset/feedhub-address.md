---
"@0xslots/contracts": minor
---

Add `feedHubAddress` export for the new FeedHub/Feed on-chain feed registry. FeedHub is deployed on Base Sepolia (`0x3B5eC015339b654F1220C32a5D29679C527Fb3B7`) with feed #0 ("The Testnet Feed") seeded from the curated 42-slot list. Base (mainnet) deploy is pending.
